`timescale 1ns/1ps

module clock_divider(
    input wire clk,
    input wire rst,
    output reg div_clk
);

always @(posedge clk or posedge rst) begin
    if (rst)
        div_clk <= 0;
    else
        div_clk <= ~div_clk; // toggles every clock edge
end

endmodule
